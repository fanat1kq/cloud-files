spring:
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create
preliquibase:
  enabled: false
secrets:
  postgres:
    username: postgres_test
    password: postgres_test
    url: jdbc:postgresql://localhost:5433/test
  minio:
    url: http://localhost:9000
    access-key: minio_test
    secret-key: minio_test_pass